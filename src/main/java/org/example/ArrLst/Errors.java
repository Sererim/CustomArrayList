package org.example.ArrLst;

/**
 * Enum for handling errors that ArrList may produce during its working.
 */
public enum Errors {
  NO_ERROR,
  INDEX_OUT_OF_BOUNDS,
  NO_SUCH_ELEMENT,
  NULL_VALUE,
  UNKNOWN_ERROR
}
